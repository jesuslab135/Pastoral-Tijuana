/** @jsxImportSource react */

// Placeholder root of the admin island: the router, the store provider and the
// screens land here in the next task. It exists now so the Astro shell, the
// React integration and the JSX pragma are proven end to end.
export default function App() {
  return null;
}
